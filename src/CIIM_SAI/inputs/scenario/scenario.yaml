# CIIM — scenario inputs
#
# What the model is being asked to do: how much is injected, where, and using what deployment method.
#
# Units are SI base units or USD unless noted otherwise in the variable name.
# deployment_pattern names a file in deployment_patterns/.

deployment_method: teleporter
deployed_material: SO2
altitude: 20000


# Exactly one of these. deployment_pattern is the override: it names the
# deployed masses directly and skips the climatology module entirely.
deployment_pattern: example_deployment_pattern.csv
# temperature_pattern: example_temperature_pattern.csv
# latitude: 15


# Optional parameter sweep capability. Each entry means a parameter sweep of the given variable. 
# Currently implemented: start/stop/step range; stop is inclusive. In a multi-dimensional sweep,
# the model simulates a scenario space with a number of dimensions equal to the number of parameters being swept.
sweep:
  scenario.altitude:
    start: 15000
    stop: 30000
    step: 1000


sweepable:
  - altitude
  - latitude
